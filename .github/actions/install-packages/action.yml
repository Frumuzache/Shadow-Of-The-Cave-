name: 'Install packages'
description: 'Install required packages for building the project'

runs:
  using: "composite"
  steps:
    # -------------------------
    # 🐧 Linux Dependencies
    # -------------------------
    - name: Install Linux Dependencies
      shell: bash
      if: runner.os == 'Linux'
      run: |
        set -eux
        sudo apt-get update -y
        sudo apt-get install -y --no-install-recommends software-properties-common
        sudo add-apt-repository -y universe
        sudo apt-get update -y

        # Install SFML (unified or split)
        if apt-cache show libsfml-dev >/dev/null 2>&1; then
          echo "Installing SFML and audio dependencies"
          sudo apt-get install -y --no-install-recommends \
            ninja-build libsfml-dev libopenal-dev libvorbis-dev libflac-dev
        else
          echo "Installing legacy SFML split packages"
          sudo apt-get install -y --no-install-recommends \
            ninja-build \
            libsfml-audio-dev \
            libsfml-graphics-dev \
            libsfml-network-dev \
            libsfml-system-dev \
            libsfml-window-dev
        fi

        sudo sysctl vm.mmap_rnd_bits=28 || true

    # -------------------------
    # 🍏 macOS Dependencies
    # -------------------------
    - name: Install macOS Dependencies
      shell: bash
      if: runner.os == 'macOS'
      run: |
        set -eux
        brew update
        brew install sfml ninja

    # -------------------------
    # 🪟 Windows Dependencies
    # -------------------------
    - name: Install Windows Dependencies
      shell: bash
      if: runner.os == 'Windows'
      run: |
        choco install sfml ninja

    # -------------------------
    # 🧰 Optional: Clang (for MSAN builds)
    # -------------------------
    - name: Install Clang ${{ matrix.clang_ver }}
      if: runner.os == 'Linux' && matrix.runs_msan == true
      uses: nick-fields/retry@v3
      with:
        shell: bash
        timeout_minutes: 5
        max_attempts: 3
        retry_on: error
        command: |
